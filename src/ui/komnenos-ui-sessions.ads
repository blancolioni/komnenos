package Komnenos.UI.Sessions is

   procedure Load_Session
     (UI   : Komnenos_UI;
      Path : String);

   procedure Save_Session
     (UI   : Komnenos_UI;
      Path : String);

end Komnenos.UI.Sessions;
